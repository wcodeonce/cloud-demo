spring:
  application:
    name: userservice # 服务名称
  profiles:
    active: dev # 环境
  cloud:
    nacos:
      server-addr: 127.0.0.1:80 # nacos 服务地址
      config:
        file-extension: yaml # 文件后缀名